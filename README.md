# 📸 Camera App

**Camera App** — это мобильное приложение на Flutter, которое позволяет пользователям делать снимки с помощью камеры устройства или выбирать изображения из галереи, сохранять их и просматривать в виде удобной сетки.

## ✨ Основные возможности

* **📷 Съемка фото**: Интеграция с камерой устройства для создания новых снимков.
* **🖼️ Галерея**: Возможность выбора изображений из памяти устройства.
* **🗂️ Сетка изображений**: Просмотр всех сохраненных фотографий на главном экране.
* **💾 Локальное хранение**: Фотографии сохраняются в локальной файловой системе приложения.
* **🧭 Навигация**: Удобная маршрутизация между экранами с использованием `go_router`.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Управление состоянием**: [Riverpod](https://riverpod.dev/) (`StateNotifier`, `Provider`) — для реактивного управления списком фотографий.
* **Камера и Галерея**: `image_picker` — для доступа к камере и медиатеке.
* **Файловая система**: `path_provider` — для сохранения файлов на устройстве.
* **Навигация**: `go_router` — декларативная маршрутизация.
* **Утилиты**: `uuid` — для генерации уникальных идентификаторов снимков.

## 📂 Структура проекта

Проект организован по функциональным слоям для чистоты и масштабируемости кода:

```text
lib/
├── controllers/      # Логика управления состоянием (PicturesNotifier)
├── core/             # Ядро приложения (настройка маршрутов GoRouter)
├── models/           # Модели данных (PictureModel)
├── providers/        # Объявления Riverpod провайдеров
├── screens/          # Экраны приложения
│   ├── main_screen.dart           # Главная точка входа с навигацией
│   ├── pictures_screen.dart       # Экран просмотра списка фото
│   └── pictures_create_screen.dart # Экран добавления нового фото
├── widgets/          # Переиспользуемые компоненты
│   ├── image_picker_widget.dart   # Виджет выбора/съемки фото
│   ├── picture_card_widget.dart   # Карточка одной фотографии
│   └── pictures_grid_view_widget.dart # Сетка фотографий
└── main.dart         # Точка входа в приложение
